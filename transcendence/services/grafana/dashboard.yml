apiVersion: 1

providers:
  - name: dashboards
    #folder: Services # The folder where to place the dashboards **Impt**
    type: file
    datasource: Prometheus
    updateIntervalSeconds: 30
    options:
      path: /etc/dashboards
